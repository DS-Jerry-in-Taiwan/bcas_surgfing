pandas
requests
beautifulsoup4
tqdm
lxml

# Brotli compression (TWSE requires this for JSON responses)
brotli

# Feapder Framework (optional - install if needed)
# feapder

# PostgreSQL Driver
psycopg2-binary

# Testing
pytest>=8.0.0

# Type Checking
mypy
types-requests

# OCR (BSR captcha solving)
dddddocr>=1.6.1

# Environment
python-dotenv

# Phase 3.0 - Analytics & Trading
numpy>=1.24.0
scikit-learn>=1.3.0
rich>=13.0.0
python-telegram-bot>=20.0
