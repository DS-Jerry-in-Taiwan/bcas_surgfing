# Phase 1.3.3 - Agent 角色職責

## 🏗️ @INFRA
- **職責**: 無。

## 📐 @ARCH
- **職責**: API 行為分析。
- **重點**: 
    - 判斷這是「靜態檔案」還是「API 串流」。(結論已定：是 API)。
    - 確認 `input_date` 的格式要求 (TPEx 通常是民國年 `YYY/MM/DD`)。

## 💻 @CODER
- **職責**: 實作 API 模擬請求。
- **重點**:
    - **Header 偽裝**: `User-Agent` 與 `Referer` 是關鍵。
    - **編碼處理**: TPEx CSV 通常是 Big5 (`cp950`)，需正確解碼後轉存 UTF-8。

## 🧪 @ANALYST
- **職責**: 驗收資料內容。
- **重點**:
    - 確認下載回來的不再是 404 HTML。
    - 確認 CSV 內容包含「可轉債」相關欄位 (避免抓成公債或股票)。

